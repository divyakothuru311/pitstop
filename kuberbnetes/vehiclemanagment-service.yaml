apiVersion: v1
kind: Service
metadata:
  name: vehiclemanagementapi-service
spec:
  selector:
    app: vehiclemanagementapi
  ports:
    - name: http
      port: 80  
      targetPort: 5000  
  type: ClusterIP  
